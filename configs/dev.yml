ENV: dev
MESSAGE: 'arrazo viado'
DYNAMO_TABLE_BOOKS: ${self:custom.dynamo-books}-local # Reference to Custom Env
SQS_QUEUE_URL: 'https://sqs.${self:provider.region}.amazonaws.com/#{AWS::AccountId}/${self:custom.sqs-logs}'
REGION: 'localhost'